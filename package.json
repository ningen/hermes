{
  "name": "hermes",
  "version": "1.0.0",
  "description": "Email processing agent using Mailgun, Cloudflare Workers, and Gemini",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply hermes-db"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.4"
  }
}
